#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <iostream>
#include <ctime>

using namespace std;

void random(int*, int);
void print(int*, int);
void inv(int*, int);
void notDown(int*, int);
void notUp(int*, int);
void (*fun(int*, int))(int*, int);

#endif